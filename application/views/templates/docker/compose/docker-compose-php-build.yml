
  php-{project}:
    restart: always
    build: image/php
    depends_on:
      - mysql-{project}
    ports:
      - {port}:80
    links:
      - mysql-{project}:db-server
    volumes:
      - "./src:/var/www/html"
